script.
  import { Router, Link, Route } from "svelte-routing";
  import Post from "./components/Post.pug"
  import Home from "./components/Home.pug"
  import Test from "./components/Test.pug"
  import Test2 from "./components/Test2.pug"
  export let url = "";

Router(url='{url}')
  Route(path='/about' component={Post})
  Route(path='/test' component={Test})
  Route(path='/test2' component={Test2})
  Route(path='/' component={Home})
  Route(path='*')
    h1 NOTFOUND
